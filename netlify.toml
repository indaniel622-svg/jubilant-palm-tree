[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: context-specific commands and settings
#[context.production]
#  command = "npm run build"

# Add security headers if desired
#[[headers]]
#  for = "/*"
#  [headers.values]
#    X-Frame-Options = "DENY"
#    X-Content-Type-Options = "nosniff"
